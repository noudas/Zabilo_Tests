-- MyModule Uninstall SQL
-- This file contains database cleanup when the module is uninstalled

-- Example: Drop custom tables if they exist
-- DROP TABLE IF EXISTS `PREFIX_mymodule_settings`;

-- Note: Configuration values are automatically cleaned up
-- by the uninstall() method in the main PHP file
