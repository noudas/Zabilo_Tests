-- MyModule Installation SQL
-- This file contains any database setup required for the module

-- Example: Create a custom table if needed
-- CREATE TABLE IF NOT EXISTS `PREFIX_mymodule_settings` (
--     `id_mymodule_setting` int(11) NOT NULL AUTO_INCREMENT,
--     `name` varchar(255) NOT NULL,
--     `value` text,
--     `date_add` datetime NOT NULL,
--     `date_upd` datetime NOT NULL,
--     PRIMARY KEY (`id_mymodule_setting`)
-- ) ENGINE=InnoDB DEFAULT CHARSET=utf8;

-- Note: For this exercise, we use PrestaShop's Configuration class
-- which doesn't require custom SQL tables
